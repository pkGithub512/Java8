package mix.questions;

public class CanWeDeclareClassAsStatic {
//we cannot declare class as static if there is only one class
	//but we can do if class is nested, that means class inside class is present
	static class TestStatic{
		//it is allowed
	}
}
